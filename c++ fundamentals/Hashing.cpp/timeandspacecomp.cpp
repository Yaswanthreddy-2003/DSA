//pushing and popping from the stack, accessing node values, etc.) are constant time operations.
//pushing values take //O(1) time complexity and popping values take O(1) time complexity.
//but pushing a string of length n will take O(n) time complexity.
//time complexity of stack is O(1).
